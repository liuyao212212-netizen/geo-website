# 2026-05-26 工作日志

## 智优康赛GEO官网 - 首页微调

### 已完成
- 需求收集表单标题从"开启 AI 增长之旅"改为"即刻开启AI增长之旅"
- 首页Hero背景从雷达扫描动画替换为紫色光柱图片背景
  - 图片路径：`/public/images/hero-bg.jpg`
  - 保留了深色渐变遮罩确保文字可读性
  - 移除了所有雷达相关的HTML和CSS代码
- 客户增长数据卡片从GlassPanel换成Uiverse风格玻璃边框光效卡片
  - conic-gradient旋转光边框 + 深色内填充
  - hover时上浮6px + 光边加速旋转 + 数字荧光加强
  - 保留原有count-up动画和数据结构
- CTA手机组件从旧版mockup替换为Uiverse风格手机
  - 紫蓝渐变外框 + 深色屏幕
  - hover时手机旋转8度 + 内部光球位移 + AI文字荧光加强
  - 屏幕显示"你好 AI" + 底部"Touch to unlock"锁图标
  - 内部紫色和橙色模糊光球装饰
- 其他板块背景图从纯色替换为蓝色渐变图片
  - 使用 `linear-gradient` 叠加深色遮罩（rgba(3,4,10,0.82-0.88)）确保文字清晰
  - hero区域不受影响（有自己更上层的hero-bg-image覆盖）
- Hero主标题再次调大：`clamp(4.5rem, 15vw, 10rem)`，删掉AI下划线
- 系统后台图从静态mockup换成Uiverse风格SVG动画组件
  - 网格背景 + 浏览器框架 + 骨架屏脉冲动画
  - 四条数据流线（trace-flow）带stroke-dashoffset流动动画
  - 3D透视hover效果（同之前mockup）
  - 标题文字改为"GEO System"
- 两个CTA按钮(预约诊断+联系我们)统一换成cssbuttons-io风格
  - 预约诊断：紫蓝渐变pill形 + 箭头图标
  - 联系我们：透明描边outline变体 + 邮件图标
  - hover时上浮3px+scale + 图标右移5px + 光泽叠层
- CTA按钮换成Uiverse Spacious74 sparkle星形图标按钮
  - ✨星形SVG图标+文字，hover时图标旋转90°+放大1.15
  - 预约诊断：渐变实底，联系我们：描边outline变体
- 导航栏增加二级下拉菜单
  - 桌面端：hover显示下拉面板（毛玻璃背景+平滑动画）
  - 移动端：点击展开/折叠子菜单（箭头旋转+max-height动画）
  - 首页无子菜单，其他5个导航均有2-5个子项
  - 导航栏CTA按钮也换成sparkle风格
- CTA按钮从sparkle换成Uiverse blob液态按钮
  - 两个blur光球(blob1青色+blob2紫色)在按钮内部缓慢飘移
  - 文字被毛玻璃.inner包裹(backdrop-filter: blur)
  - hover时光球显现+按钮上浮+scale放大
  - 预约诊断：渐变实底，联系我们：描边outline变体
  - 同步替换了Header导航栏的CTA按钮
- Hero背景添加Uiverse dexter-st风格loader动画
  - 三个发光圆点(紫→蓝→青)在背景中央脉冲跳动
  - 带延迟错开动画+光晕box-shadow
  - 叠在hero-bg-image之上、hero-bg-overlay之下
  - 0.45透明度不干扰前景内容
- 构建成功，dist输出正常
- CTA按钮从blob换成cssbuttons-io简洁风格
  - padding: 10px 24px，border-radius: 8px微圆角
  - hover上浮2px + 阴影加深 + 光泽从左滑到右
  - 预约诊断：渐变实底，联系我们：透明+紫色描边outline变体
- Hero背景从图片+dexter-st圆点loader换成andrew-manzyk几何mask加载器
  - SVG三角形嵌套几何mask + 紫蓝青渐变旋转动画
- 安装motion-v动画库(v2.2.1)
  - 用Motion组件包裹hero标题/副标题/搜索栏/按钮
  - 依次延迟入场(0s→0.2s→0.4s→0.6s)从下方淡入上滑
  - cubic-bezier(0.16, 1, 0.3, 1)弹性缓动
- Hero背景最终换成Three.js Beam着色器(BeamBackground.vue)
  - 体积光束+雾气+粒子流(wisps)着色器效果
  - 颜色#7b61ff紫色，匹配设计语言
  - alpha: true透明背景叠加在深色底上
  - 鼠标互动：光束随鼠标微倾
  - FPS自适应DPR调整
  - 安装three@0.184.0 + @types/three
- 所有卡片替换为vue-bits GlassSurface组件
  - SVG filter displacement map实现光学玻璃折射
  - 替换了：crisis-alert、feature-card(GlassPanel)、terminal-card、stat-card、testimonial-card、cta-panel
  - 移除了旧的conic-gradient旋转边框(.glass::before)和glassRotate keyframe
  - Chrome支持SVG filter折射效果，Safari/Firefox自动fallback到普通backdrop-filter
  - 组件参数：borderRadius、backgroundOpacity、blur、saturation等

### 文件变更
- `src/components/ContactForm.vue` - 标题文案
- `src/views/HomeView.vue` - Hero背景图替换 + stat-card组件重构 + 标题字号调大 + testimonials轮播重构 + section背景图替换
- `public/images/hero-bg.jpg` - 新增背景图片
- `public/images/section-bg.png` - 新增板块背景图片

---

## 智优康赛GEO官网 - Hero背景：Beam→Orb

### 已完成
- Hero背景从Three.js Beam着色器换成vue-bits Orb组件(OrbBackground.vue)
  - 使用ogl轻量WebGL库替代three.js（体积更小）
  - 安装ogl依赖
  - 着色器：simplex noise 3D + YIQ色相旋转 + 体积光照
  - 默认紫色(baseColor1=rgb(156,67,255) + baseColor2=cyan + baseColor3=深蓝)
  - hue=0保持紫色，hoverIntensity=0.5增强hover效果
  - 鼠标hover时球体扭曲+旋转，球体内部光点绕行
  - 透明背景(alpha: true)叠加在深空黑上
- 创建`src/components/OrbBackground.vue`，替换`BeamBackground`引用
- 构建成功
- 修复Orb不可见问题：hero-bg-overlay z-index从0→1使其覆盖Orb(z:0)但更透明，Orb容器去掉pointer-events:none使鼠标交互可用
- 三个板块从GlassSurface换回普通div：客户评价、GEO五步法、为什么选择智优康赛
  - feature-card加回背景+边框+hover样式
  - terminal-card加回背景+边框+hover样式
  - testimonial-slide恢复原始结构
- **修复Orb紫色球体不可见问题（根因找到！）**
  - 用户直接提供了vue-bits Orb组件完整代码
  - 问题根因：Orb组件内部`.orb-container { position: relative }` scoped样式覆盖了HomeView中`.hero-orb-bg { position: absolute }`
  - 导致容器没有正确填满hero区域，canvas尺寸只有577px高而非全屏
  - 修复：移除Orb组件内部的`position: relative`和`overflow: hidden`，让外部CSS控制定位
  - 同时给canvas加`position: absolute; top:0; left:0`确保填满
  - 设置`forceHoverState=true`让球体始终可见（不需要hover才显示）
  - 调整overlay透明度更通透，让紫色球体更突出
  - 截图验证：紫色球体在Hero区域中央清晰可见，带有柔和光晕和体积感

## 智优康赛GEO官网 - 为什么选择智优康赛板块：BorderGlow

### 已完成
- 创建`src/components/BorderGlow.vue`组件
  - 鼠标跟随光晕边框效果（conic-gradient + cursor angle tracking）
  - mesh gradient填充（7个径向渐变叠加）
  - 外发光（box-shadow多层 + plus-lighter混合）
  - 入场扫描动画（animated=true时自动播放sweep）
  - 配色：紫色系 #a855f7 + #6366f1 + #38bdf8
  - backgroundColor: rgba(6, 0, 16, 0.85)，glowColor: 260 80 65（紫色HSL）
- 为什么选择智优康赛板块feature-card从普通div换成BorderGlow组件
  - 去掉了feature-card自带的background/border/border-radius（BorderGlow自带）
  - 保留hover translateY(-4px)效果

## 智优康赛GEO官网 - 品牌危机板块：NotificationCard

### 已完成
- 创建`src/components/NotificationCard.vue`组件（Uiverse SouravBandyopadhyay风格）
  - notiglow：内部径向发光（hover时opacity增强0.25→0.6）
  - notiborderglow：边框外发光（hover时opacity 0→0.3）
  - 左侧3px色条（warning=黄/error=红/info=蓝/success=绿）
  - 标题带图标（warning三角/error叉/info圈/success勾）
  - 标签pill用对应级别的半透明色+accent文字色
  - hover时translateX(6px)右移效果
- 危机卡片从GlassSurface换成NotificationCard
  - 4种级别（warning/error/info/success）各有独立的glow/border/accent配色
  - 去掉了旧的.crisis-alert和.crisis-alert__*系列CSS（NotificationCard自带scoped样式）

## 智优康赛GEO官网 - 联系表单Logo替换

### 已完成
- ContactForm.vue左上角.brand-header从占位SVG(圆圈)换成公司真实logo
  - 复制`/Users/rose/Desktop/智优康赛/智优康赛logo_1主logo白.svg`到`/public/images/logo-white.svg`
  - 用`<img>`标签替换旧的`<svg>`+`<span>`
  - logo高度28px，带紫色drop-shadow光效
  - 去掉了旁边的"智优康赛"文字span（logo本身已包含文字）

## 智优康赛GEO官网 - 联系表单：Chat Bot风格

### 已完成
- ContactForm.vue从左右分栏3步表单完全替换为Uiverse Cobp风格Chat Bot
  - 单卡片布局，深空黑底+紫蓝渐变边框
  - 顶部：公司logo + 标题"开启AI增长之旅"
  - AI平台标签选择区（8个平台，多选）
  - 品牌痛点标签选择区（4个痛点，单选）
  - Chat Bot输入框（Uiverse Cobp风格）：
    - textarea + placeholder "描述您的需求...✦˚"
    - 左侧3个功能按钮（📎附件/🖼图片/🌐全球）
    - 右侧紫蓝渐变发送按钮（纸飞机SVG图标）
    - focus时边框发光+box-shadow
  - 快捷标签区：3个预设短语点击自动填入textarea
  - 提交后显示成功状态
  - 去掉了旧的3步表单逻辑（currentStep/nextStep/prevStep）
  - 去掉了基本信息收集（公司/姓名/职位/电话/邮箱输入框）

## 智优康赛GEO官网 - 表单还原 + 移动端公司信息 + Hero字号

### 已完成
- ContactForm.vue从Chat Bot风格换回3步分栏表单
  - Step 1: 基本信息（公司/姓名/职位/电话/邮箱，2列grid布局）
  - Step 2: AI平台标签多选 + 品牌痛点单选
  - Step 3: 补充说明textarea
  - 步骤指示器（3个圆点+渐变进度条）
  - 上一步/下一步/提交咨询按钮
  - 保留成功状态动画
- Hero主标题字号缩小：clamp(4.5rem,15vw,10rem)→clamp(3.2rem,10vw,7rem)
- GEO五步法terminal-code隐藏滚动条
- 移动端导航栏(SiteHeader)底部增加公司信息
  - 耐特康赛（母公司）：耐特康赛网络技术（北京）有限公司 | netconcepts.cn
  - 智优康赛（子公司）：北京智优康赛科技有限公司 | GEO业务独立运营
  - 总部：中国
  - 商务合作CTA：欢迎垂询，共同探索AI搜索时代的品牌增长路径
  - 样式：紫色标签+灰色文字+底部CTA卡片

## 智优康赛GEO官网 - 资源中心页面

### 已完成
- 创建`src/views/ResourcesView.vue`，包含3个子板块
  1. **GEO白皮书**：左文右图布局，2张封面图3D透视hover效果
  2. **GEO创新联盟(CGIA)**：7大权益卡片网格+加入联盟卡片(会员类型+二维码)
  3. **GEO常见问题(FAQ)**：手风琴折叠模式，点击展开答案，Q/A图标badge
- App.vue集成ResourcesView组件，id="resources"锚点匹配导航
- 构建验证通过

### Aurora背景 + BlurText动画升级
- 创建`src/components/AuroraBackground.vue`（ogl WebGL极光着色器）
  - simplex noise 3D + color ramp渐变 + 体积光
  - 三个section标题区域各配独立极光配色：
    - 白皮书：紫→深空→蓝 [#4b33ff, #0a0b14, #4c75ff]
    - GEO联盟：青→深空→紫 [#00d4ff, #0a0b14, #4b33ff]
    - FAQ：蓝→深空→青 [#4c75ff, #0a0b14, #00d4ff]
  - amplitude/blend/speed/intensity参数微调
- 创建`src/components/BlurText.vue`（motion-v BlurText逐词模糊入场）
  - direction: top/bottom，animateBy: words/letters
  - delay/stepDuration/threshold可配
  - IntersectionObserver触发，进入视口才播放
- ResourcesView全面集成：
  - 3个section标题区域用Aurora极光背景
  - 所有文本（标题/副标题/描述/权益标题/FAQ问题/答案）均用BlurText包裹
  - 构建验证通过

### 多页面路由重构
- 安装 vue-router@4，创建 `src/router/index.ts`
  - 6个路由：/ (Home) /solutions /cases /platform /about /resources
  - createWebHashHistory（兼容GitHub Pages部署）
  - scrollBehavior: 每次跳转回到顶部
  - beforeEach: 动态设置document.title
  - 懒加载所有页面组件（代码分割）
- App.vue 从直接嵌入HomeView+ResourcesView 改为 `<router-view>`
- SiteHeader 全面重构：
  - navLinks数据从 href="#xxx" 改为 routeName: 'Solutions' 等
  - Logo换成 `<router-link :to="{name:'Home'}">`
  - 无子菜单项用 `<router-link>`，有子菜单的父级用 `<a>` + toggleDropdown
  - 下拉子项和移动端子项都通过 `navigateTo(routeName)` 跳转
  - 新增 `isActive(routeName)` 判断当前路由高亮
  - router-link-active 状态下加下划线+高亮色
- 创建4个占位页面：SolutionsView / CasesView / PlatformView / AboutView
  - 每个都有Aurora极光Hero + BlurText标题 + "页面建设中"占位
  - 各自独立配色
- 构建验证通过，代码分割正常（每个页面独立chunk）

### 资源中心背景替换 + TiltedCard动效升级
- 复制 `6C.png` → `/public/images/section-bg.png`（35MB大图）
- ResourcesView三个section标题区：AuroraBackground → 图片背景
  - CSS `filter: brightness(0.22) contrast(1.4) saturate(0.6)` 大幅调暗白色/亮色
  - 叠加深色渐变遮罩 `rgba(3,4,10,0.35~0.6)` 确保与深空黑底色融合
  - 文案一字未缩减，全部保留
- 创建 `src/components/TiltedCard.vue`（motion-v弹簧动画）
  - handleMouse: 根据鼠标位置计算rotateX/rotateY（±12deg振幅）
  - handleMouseEnter: scale放大到1.08
  - handleMouseLeave: 所有值归零，弹簧过渡回弹
  - tooltip跟随鼠标位置（弹簧动画）
- ResourcesView白皮书封面：cover-card CSS hover → TiltedCard组件
  - 每张180×240px，scaleOnHover: 1.08, rotateAmplitude: 12
  - 封面下方保留标题/描述文字（cover-meta）
- 构建验证通过

### 修复导航跳转问题
- **问题根因**：SiteFooter底部导航全是`<a href="#">`，没有任何路由跳转逻辑
- **SiteHeader顶部导航**：经Playwright实测，路由跳转正常（点击"资源中心"→URL变为`/#/resources`）
- **SiteFooter修复**：
  - footerNav数据结构重构：每个栏目增加`routeName`，子链接增加`{label, routeName}`对象
  - 引入`useRouter`，添加`navigateTo(routeName)`方法
  - 栏目标题`<h4>`和子链接`<a>`都绑定`@click="navigateTo(...)"`
  - 添加`cursor: pointer`和hover颜色变化
- Playwright端到端验证：顶部导航+底部导航点击"资源中心"均成功跳转

### 资源中心Hero升级：MagicRings + 大Logo + BlurText
- 创建 `src/components/MagicRings.vue`（Three.js WebGL魔法光环）
  - 6层光环，紫→青色渐变，跟随鼠标，hover缩放
  - 参数：color=#4b33ff, colorTwo=#00d4ff, ringCount=5, followMouse=true
- ResourcesView Hero区重构：
  - 背景：AuroraBackground → MagicRings（Three.js）
  - Logo：40px → 56px（desktop），28px → 36px（mobile）
  - 标题：从纯h1 → BlurText逐词模糊入场动画
  - 整体min-height: 280px → 320px，更大气
  - 间距gap: $space-6 → $space-8
- 记住此布局模式（Logo+竖线+标题），后续各板块复用

### 资源中心背景+卡片风格统一
- **Section标题背景**：从 section-bg.png → resources-bg.png（渐变图）
  - filter: brightness(0.18) contrast(1.3) saturate(0.5) 更暗
  - 渐变遮罩 rgba(3,4,10,0.65~0.7) 更深，与#03040a底色融合
- **所有卡片统一液态玻璃风格**：
  - 权益卡片：已有 GlassSurface ✅
  - 加入联盟卡片：已有 GlassSurface ✅
  - FAQ手风琴：CSS backdrop-filter blur(20px) → 液态玻璃效果
  - 白皮书要点：新增 GlassSurface 包裹每个highlight-item
- 构建验证通过

### 修复手机端导航跳转bug
- **问题**：手机端移动菜单中，有子菜单的导航项（解决方案/客户案例/智优平台/关于我们/资源中心）点击只展开子菜单，不触发页面跳转
- **根因**：SiteHeader.vue移动端 `@click="toggleDropdown('m-' + link.label)"` 只切换子菜单展开/折叠
- **修复**：改为 `@click="navigateTo(link.routeName)"` 直接跳转页面
- 子菜单项 `@click` 加 `.stop` 修饰符防止冒泡
- Playwright iPhone X (375×812) 模拟验证通过：首页→点击资源中心→URL从`/#/`变为`/#/resources`

### 白皮书封面：TiltedCard→BookCard 3D翻书动画
- 创建 `src/components/BookCard.vue`（Uiverse eslam-hany 风格 3D 翻书）
  - 封面：img 图片 + 左侧 spine 紫色渐变 + hover 光泽扫过动画
  - 内页：3层堆叠书页 + 背面内容区（书名+副标题）
  - hover 时封面沿左侧轴 rotateY(-170deg) 翻开，露出内页
  - 紫色发光阴影随 hover 加深扩散
  - 移动端：rotateY(-25deg) 微翻，避免完全遮挡
  - 深空黑主题配色：内页背景 rgba(20,16,40)，书名渐变紫→青
- ResourcesView.vue：TiltedCard → BookCard，调整 gap 和 meta 间距
- Playwright 桌面端+移动端验证通过

### 资源中心布局重构：FAQ + 联盟
- **FAQ常见问题**：从居中手风琴 → 左右分栏布局（参考图1）
  - 左侧：sticky 固定大标题"常见问题解答"
  - 右侧：手风琴折叠列表，细边框分隔线（去圆角+去毛玻璃背景）
  - 去掉 Q/A badge 图标，简化问题行
  - 移动端：垂直堆叠，标题不 sticky
- **GEO创新联盟**：从网格卡片 → 左右分栏时间线布局（参考图2）
  - 创建 `src/components/TimelineSteps.vue` 组件
    - 左侧编号圆圈（1-7）+ 紫色连接线
    - 右侧内容卡片（icon + title + desc）
    - hover 时卡片右移 + 编号放大 + 边框高亮
    - 深黑卡片背景 + 细边框
  - 左侧：sticky 标题"联盟权益" + 描述文字
  - 右侧：TimelineSteps 组件展示7大权益
  - 底部保留：加入联盟卡片（会员类型 + 二维码）
  - 移动端：垂直堆叠，标题不 sticky
- 构建验证通过

### 资源中心背景图：从标题区移到整个section
- **问题**：背景图resources-bg.png只在section-header（标题区域）显示，用户要求整个板块都有背景图
- **修复**：
  - 删除`.section-header`和`.section-header-bg`的背景图逻辑
  - 新增`.section-bg`：`position: absolute; inset: 0` 覆盖整个section
  - 新增`.section-content`：`position: relative; z-index: 2` 承载所有内容
  - 三个section（白皮书/联盟/FAQ）结构：`<section> → .section-bg + .section-content → 所有内容`
  - `.section-header-content`变为普通内容区域，不再有独立min-height
  - CSS filter/overlay参数不变：brightness(0.55) contrast(1.1) saturate(0.85) + 渐变遮罩
- 构建验证通过

- 构建验证通过

---

## 智优康赛GEO官网 - 智优平台页面

### 已完成
- 创建`src/views/PlatformView.vue`，包含4个板块 + Hero区
  1. **智优系统**：SplashCursor WebGL流体标题 + 6大系统能力3列GlassSurface卡片网格
     - AI语义理解引擎、实时监控中心、智能诊断分析、自动化优化引擎、效果追踪系统、竞品防御体系
     - 大字"ZhiYou System"标题 + SplashCursor流体背景（复用CGIA标题模式）
  2. **核心功能**：居中section-title + 左右分栏（左侧sticky"功能全景"标题 + 右侧TimelineSteps 5步时间线）
     - AI可见度监测、语义结构优化、精准内容训练、实时策略迭代、GEO诊断报告
     - 复用ResourcesView联盟板块的grid布局模式
  3. **系统优势**：4列数据卡片（大数字+单位+标签+描述）
     - 7大AI平台/50+优化维度/24/7实时监控/3×效果提升
     - 数字用gradient渐变色，hover上浮6px+scale放大+紫色阴影
  4. **系统赋能**：3列行业赋能卡片（icon+标题+描述+标签pills）
     - 企业品牌/电商零售/B2B服务/医疗健康/教育科技/金融服务
     - 每个卡片3个标签pills，hover时标签边框高亮变色
- Hero区：复用资源中心MagicRings+Logo+竖线+BlurText标题模式
- 所有section背景图复用resources-bg.png，section-bg+section-content分层结构
- 移动端响应式：3列→2列→1列网格，sticky标题降级为static
- Vite编译无错误，页面可通过 `/#/platform` 路由访问

### CGIA联盟标题用SplashCursor WebGL流体效果
- 创建 `src/components/SplashCursor.vue`（WebGL流体模拟组件）
  - 从原始全屏fixed定位改为容器内relative定位
  - 事件监听从window改为container局部
  - 添加auto-splat自动飞溅效果（每1.5秒随机splat）
  - onUnmounted清理动画帧+定时器+事件监听
- CGIA标题区重构：
  - "GEO创新联盟" → "CGIA"，超大字号clamp(4rem,12vw,8rem)
  - 字间距0.15em，text-shadow紫色光晕
  - 副标题"深度共建 GEO 生态，赋能AI 营销时代"
  - 左侧标题从"联盟权益"+描述 → 只有"核心权益"
- 构建验证通过

### CGIA联盟加入板块重构：Glass卡片+二维码
- 底部加入联盟区域从GlassSurface大卡片改为：两个glass倾斜卡片 + 二维码
  - 卡片1："创始会员单位"，卡片2："创始会员先锋"
  - Uiverse glass风格：backdrop-filter blur + 紫蓝渐变overlay + 光泽扫过动画
  - 默认倾斜（--r: -8deg / 5deg），hover时归零并放大
  - 移除了旧的"单位会员/先锋会员"badge + 描述文字 + "加入GEO创新联盟"标题
- 二维码替换为用户提供的CGIA官方二维码图片
  - 原路径：`/images/qr-cgia.png` → 新路径：`/images/qr-cgia-new.jpg`
  - 尺寸140px，白色背景padding，hover放大效果
- 构建验证通过

### 智优平台标题区：SplashCursor → FloatingLines
- 创建 `src/components/FloatingLines.vue`（Three.js WebGL浮动光线着色器）
  - 3层波浪线（top/middle/bottom），每层可配lineCount/lineDistance/wavePosition
  - 支持 linesGradient 渐变色数组（最多8个stop）
  - 鼠标交互：bend弯曲 + parallax视差
  - mixBlendMode: screen 在深色背景上发光
- PlatformView 智优系统标题区替换：
  - SplashCursor → FloatingLines
  - 渐变配色：#4b33ff(紫) → #4c75ff(蓝) → #00d4ff(青)
  - lineCount: [4, 8, 4]，lineDistance: [8, 4, 6]
  - animationSpeed: 0.8（稍慢，更优雅）
  - system-header加背景渐变（深空黑→深蓝→深空黑），让screen混合模式正确发光
  - min-height: 280px → 320px（desktop），200px → 240px（mobile）
- Vite编译零错误

### 智优平台：GEO工作流交互演示（第一版）
- 创建 `src/components/GeoWorkflow.vue`（纯Vue3+SVG+CSS）
  - 5个可拖拽节点，SVG贝塞尔连线，连线动画，右侧运行面板
  - 删除"ZhiYou System"英文大标题

### 智优平台：GEO工作流重构为自动播放垂直演示
- **重写 `src/components/GeoWorkflow.vue`**：
  - 删除所有拖拽交互，改为**垂直布局自动循环播放**
  - 参照图2节点编辑器样式：深色卡片垂直排列，节点间有+号连接点和连线
  - **自动演示**：每步2.5s高亮→连线粒子流动→下一步，循环间隔1.5s
  - 节点卡片：序号徽标+标题+英文副标题+Output标签+子项标签
  - 连线动画：粒子从上方节点流向下方节点（CSS动画）
  - 右侧状态面板：步骤列表+进度条+播放/暂停按钮
  - 移动端：面板移到下方，垂直布局自适应
- **PlatformView.vue修改**：
  - 删除整个 `system-header` 标题区（FloatingLines+文字全部删除）
  - 删除6能力卡片的 `desc` 说明文字，只保留icon+title
  - 板块一只保留 GeoWorkflow + 6个无描述的能力卡片
- Vite编译零错误

### 智优平台：删除6个能力卡片
- 删除 `systemCapabilities` 数据数组和 `system-grid` 卡片网格
- 删除所有 `.system-grid`、`.capability-card`、`.capability-inner`、`.capability-icon`、`.capability-title`、`.capability-desc` CSS
- 板块一现在只保留 GeoWorkflow 自动播放工作流演示
- Vite编译零错误

### 智优平台：删除系统赋能板块描述文字
- 删除6个行业卡片中的 `emp.desc` BlurText 描述文字
- 删除 `.empower-desc` CSS 样式
- 行业卡片现在只保留 icon + title + tags
- Vite编译零错误

### 智优平台：系统赋能板块替换为 CircularGallery
- 创建 `src/components/CircularGallery.vue`（ogl WebGL 3D弧形画廊）
  - 6个行业图片+文字：企业品牌/电商零售/B2B服务/医疗健康/教育科技/金融服务
  - ogl WebGL渲染，3D弧形布局(bend=3)，圆角卡片(borderRadius=0.05)
  - 鼠标拖拽+滚轮+触摸交互，平滑滚动(scrollEase=0.05)
  - 顶点着色器波浪动画(uTime+uSpeed)
  - 片段着色器圆角SDF裁剪
- PlatformView.vue替换：
  - 删除6行业卡片empowerments数据和empower-grid模板
  - 删除所有empower相关CSS（.empower-grid/.empower-card/.empower-inner/.empower-icon/.empower-title/.empower-tags/.empower-tag）
  - 新增.gallery-container容器（500px高，移动端350px）
  - 删除副标题"多行业GEO优化方案..."
- Vite编译零错误
